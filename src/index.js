import React from 'react';
import ReactDOM from 'react-dom';
import './Style/index1.css'; // וודא שהנתיב נכון

import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
